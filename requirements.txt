# requirements.txt - Python项目依赖列表 (最终修正版本，兼容Python 3.12)

# FastAPI及其ASGI服务器
fastapi==0.115.0
uvicorn[standard]==0.32.0

# 数据处理和科学计算
pandas==2.2.2
numpy==1.26.4

# Redis客户端
redis==5.1.1

# 数据源库
# Tushare (需要自行注册并获取token)
tushare==1.4.21
# Akshare (可能需要安装一些C++编译工具，例如Windows上的Visual C++ Build Tools)
akshare==1.16.99
# Yahoo Finance
yfinance==0.2.40
# 加密货币交易所接口
ccxt==4.3.11

# 配置管理
dynaconf==3.2.5

# 模板引擎
Jinja2==3.1.4

# 异步HTTP客户端
aiohttp==3.11.13 # <-- 已更新到 akshare 所要求的最低版本，且存在于PyPI

# 健壮性: 重试机制
tenacity==8.2.3

# 序列化和压缩
msgpack==1.0.8

# 监控 (可选，如果不需要Prometheus监控可以不安装)
prometheus_client==0.20.0

# K线图绘制
mplfinance==0.12.10b0
matplotlib==3.8.4
pillow==10.3.0

# JWT认证
PyJWT==2.8.0

# 结构化日志
structlog==25.4.0

